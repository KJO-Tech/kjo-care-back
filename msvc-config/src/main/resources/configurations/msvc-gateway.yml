eureka:
  client:
    register-with-eureka: false

server:
  port: 8080

spring:
  application:
    name: msvc-getaway
  cloud:
    config:
      discovery:
        enabled: true
    gateway:
      mvc:
        routes: